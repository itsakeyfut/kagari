[package]
name = "kagari-widgets"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
kagari-base = { workspace = true }
kagari-core = { workspace = true }
kagari-style = { workspace = true }

[dev-dependencies]
# Downward-only (widgets → core → {layout, render, text}); test-harness only: build/paint an element
# tree (render_tree) and assert scene/behavior. Not part of the public dependency surface.
kagari-layout = { workspace = true }
kagari-render = { workspace = true }
kagari-text = { workspace = true }

[lints]
workspace = true
